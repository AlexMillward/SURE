<states>
  <itno>0</itno>
  <agents>

    <xagent>
      <name>Division</name>

      <!-- == basic typed == -->

      <!-- Identity details -->
      <firm_id>0</firm_id>
      <id>1</id>

      <!-- Production details -->
      <output_good_id>0</output_good_id>
      <production_costs_per_unit>2</production_costs_per_unit>

      <!-- == data structures == -->

      <!-- (current, target) -->
      <output_inventory>{0,30}</output_inventory>

      <!-- [(good id, current, target, ordered, restock_level, requirement_per_unit_output)] -->
      <production_inventory>{{1, 10, 30, 0, 20, 1}}</production_inventory>

      <!-- (unit) -->
      <division_costs>{2}</division_costs>

      <!-- (production) -->
      <funding>{10000}</funding>

      <!-- (quantity, production_per_unit) -->
      <physical_capital>{2,5}</physical_capital>

      <!-- (quantity, production_per_unit) -->
      <human_capital>{5,2}</human_capital>

      <!-- leave alone, computed during iteration (length 6) -->
      <output_information>{0,0,0,0,0,0}</output_information>

    </xagent>

    <xagent>
      <name>Division</name>

      <!-- == basic typed == -->

      <!-- Identity details -->
      <firm_id>0</firm_id>
      <id>2</id>

      <!-- Production details -->
      <output_good_id>1</output_good_id>
      <production_costs_per_unit>2</production_costs_per_unit>

      <!-- == data structures == -->

      <!-- (current, target) -->
      <output_inventory>{0,30}</output_inventory>

      <!-- [(good id, current, target, ordered, restock_level, requirement_per_unit_output)] -->
      <production_inventory>{}</production_inventory>

      <!-- (unit) -->
      <division_costs>{2}</division_costs>

      <!-- (production) -->
      <funding>{10000}</funding>

      <!-- (quantity, production_per_unit) -->
      <physical_capital>{2,5}</physical_capital>

      <!-- (quantity, production_per_unit) -->
      <human_capital>{5,2}</human_capital>

      <!-- leave alone, computed during iteration (length 6) -->
      <output_information>{0,0,0,0,0,0}</output_information>

    </xagent>

  </agents>
</states>
