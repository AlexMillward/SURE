<states>
  <itno>0</itno>
  <agents>

    <xagent>
      <name>Division</name>

      <!-- == basic typed == -->

      <!-- Identity details -->
      <firm_id>0</firm_id>
      <id>1</id>

      <!-- Production details -->
      <output_good_id>0</output_good_id>
      <production_costs_per_unit>2</production_costs_per_unit>

      <!-- == data structures == -->

      <!-- (current, target) -->
      <output_inventory>{0,30}</output_inventory>

      <!-- [(good id, current, target, ordered, restock_level, requirement_per_unit_output, latest_unit_cost)] -->
      <production_inventory>{{1, 10, 30, 0, 20, 1, 5}}</production_inventory>

      <!-- (unit) -->
      <costs>{2,0}</costs>

      <!-- (production) -->
      <funding>{10000}</funding>

      <!-- (quantity, production_per_unit) -->
      <physical_capital>{2,5}</physical_capital>

      <!-- (quantity, production_per_unit, mean_wage) -->
      <human_capital>{5,2,0}</human_capital>

      <!-- leave alone, computed during iteration (length 6) -->
      <output_information>{0,0,0,0,0,0}</output_information>

      <!-- (position_x, position_y, vehicle_speed, vehicle_capacity, vehicle_cost_per_day, unit_cost_per_day) -->
      <transport_information>{0,0,1,0}</transport_information>

      <!-- leave alone, computed during iteration (item length 4) -->
      <deliveries>{}</deliveries>

    </xagent>

    <xagent>
      <name>Division</name>

      <!-- == basic typed == -->

      <!-- Identity details -->
      <firm_id>0</firm_id>
      <id>2</id>

      <!-- Production details -->
      <output_good_id>1</output_good_id>
      <production_costs_per_unit>2</production_costs_per_unit>

      <!-- == data structures == -->

      <!-- (current, target) -->
      <output_inventory>{0,30}</output_inventory>

      <!-- [(good id, current, target, ordered, restock_level, requirement_per_unit_output, latest_unit_cost)] -->
      <production_inventory>{}</production_inventory>

      <!-- (unit) -->
      <costs>{2,0}</costs>

      <!-- (production) -->
      <funding>{10000}</funding>

      <!-- (quantity, production_per_unit) -->
      <physical_capital>{2,5}</physical_capital>

      <!-- (quantity, production_per_unit, mean_wage) -->
      <human_capital>{5,2,0}</human_capital>

      <!-- leave alone, computed during iteration (length 6) -->
      <output_information>{0,0,0,0,0,0}</output_information>

      <!-- (position_x, position_y, vehicle_speed, vehicle_capacity, vehicle_cost_per_day, unit_cost_per_day) -->
      <transport_information>{2,2,1,0}</transport_information>

      <!-- leave alone, computed during iteration (item length 4) -->
      <deliveries>{}</deliveries>

    </xagent>

  </agents>
</states>
